@model List<MaquinaModel>
@{
    ViewData["Title"] = "Listagem de Maquinas";
}
<div class="text-center">
    <div class="d-grid gap-2 d-md-flex justify-content-md-start">
        <a class="btn btn-primary" role="button" asp-controller="Maquina" asp-action="Criar">Adicionar nova maquina</a>
    </div>

    <br />

    @if (TempData["MensagemSucesso"] != null)
    {
        <div class="alert alert-success" role="alert">
            <button type = "button" class="btn btn-danger btn-sm close-alert" arial-label="Close">X</button>
            @TempData["MensagemSucesso"]
    </div>
    }
    @if (TempData["MensagemError"] != null)
    {
        <div class="alert alert-danger" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert" arial-label="Close">X</button>
            @TempData["MensagemError"]
        </div>
    }

    <br />
    <br />

    <h1 class="display-4"> Listagem de Maquinas</h1>

    <table class="table" id="table-maquinas">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nome</th>
                <th scope="col">Seção</th>
                <th scope="col">Descrição</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @if(Model != null && Model.Any())
            {
                foreach (MaquinaModel maquina in Model)
                {
                    <tr>
                        <th scope="row">@maquina.Id</th>
                        <td>@maquina.Nome</td>
                        <td>@maquina.Secao</td>
                        <td>@maquina.Descricao</td>
                        <td>
                            <div class="btn-group" role="group">
                                <a role="button" class="btn btn-primary" asp-route-id="@maquina.Id" asp-controller="Maquina" asp-action="Editar">Editar</a>
                                <a role="button" class="btn btn-danger"  asp-route-id="@maquina.Id" asp-controller="Maquina" asp-action="ApagarConfirmacao">Apagar</a>
                            </div>
                        </td>
                    </tr>
                }
            }
            

        </tbody>
    </table>
</div>

